# Dev-only: crypto-browserify (vite polyfills) → elliptic — GHSA-848j-6mx2-7j84 has no fix on npm for ≤6.6.1 (doar low).
# `npm install` afișează în continuare audit-ul; exit code 0 când sunt doar low (nu blochează CI).
# `npm run audit:prod` = 0 vuln în arborele de producție; `npm run audit:dev` = audit complet.
audit-level=moderate
